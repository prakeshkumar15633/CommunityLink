apiVersion: v1
kind: Service
metadata:
  name: communitylink-service
  namespace: communitylink-ns
spec:
  type: NodePort
  selector:
    app: communitylink
  ports:
    - port: 4000
      targetPort: 4000
      nodePort: 31818
